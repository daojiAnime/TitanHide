; Copy to providers.ini and edit for L3_multi_provider.bat (optional).
; This file is a template only — no exploit payloads.

[default]
; External mapper (full path)
mapper=
; Optional args before TiDaoji.sys
args=
; Example provider id for multi-provider mappers (tool-specific)
provider=0

; Examples (fill yourself; do not commit secrets or stolen drivers)
; mapper=C:\lab\kdmapper.exe
; mapper=C:\lab\custom_mapper.exe
; args=-scv 1
